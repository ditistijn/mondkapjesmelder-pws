import 'package:mondkapjesmelder/logic/web3/deets.dart';

Future getUserType() async { 
  //String type = await dbInstance.getUserAdmin(credentials.address, credentials: credentials);
  //print(type);
  //return type;
  //implement getUserType
  return true;
}
